<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC
		   "-//mybatis.org//DTD Mapper 3.0//EN" 
		   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="userrank_mapper">
	<insert id="saveUserRank" parameterType="UserRankDTO">
		INSERT INTO LQ_USER_RANK (
		rank_id,
		user_id,
		total_exp,
		ranking
		) VALUES (
		SEQ_LQ_USER_RANK_PK.nextval,
		#{userId},
		#{totalExp},
		#{ranking} )
	</insert>
</mapper>
