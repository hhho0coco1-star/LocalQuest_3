<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC
		   "-//mybatis.org//DTD Mapper 3.0//EN" 
		   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="business_mapper">
	<insert id="saveBusiness" parameterType="BusinessDTO">
		INSERT INTO LQ_BUSINESS (
		business_id,
		user_id,
		business_name,
		zip_code,
		address,
		address_detail,
		phone,
		description,
		created_at
		) VALUES (
		SEQ_LQ_BUSINESS_PK.nextval,
		#{userId},
		#{businessName},
		#{zipCode},
		#{address},
		#{addressDetail},
		#{phone},
		#{description},
		sysdate )
	</insert>
</mapper>
